import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#16202b",
    },
    secondary: {
      main: "#155fa0",
    },
    white: {
      main: "FFFFFF",
    },
  },
});

export default theme;
